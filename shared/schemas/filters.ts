// Re-exported from loan.ts (where it's colocated with the loan types it
// filters) so call sites can import from a stable `shared/schemas/filters` path.
export { loanFiltersSchema, type LoanFilters } from './loan'
